import React from 'react';

const Login = () => {
    return (
        <div>
            <button className='btn btn-primary px-8'>Sign in with google</button>
        </div>
    );
};

export default Login;